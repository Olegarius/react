import pageReducer, * as pageActions from './page';

const { INIT_PAGES, SET_PAGE, SET_SETTINGS, initPages, setPage, setSettings } = pageActions;

export { INIT_PAGES, SET_PAGE, SET_SETTINGS, initPages, setPage, setSettings };
export default pageReducer;
